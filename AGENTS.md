# AGENTS.md - Antigravity 개발 및 협업 지침서

이 문서는 AI 코딩 어시스턴트인 **Antigravity**가 본 프로젝트를 개발, 수정, 검증할 때 준수해야 하는 행동 규칙 및 개발 지침을 정의합니다.

---

## 1. 프로젝트 참고 문서

프로젝트의 상세 설계, 기술 사양 및 요구사항은 다음 문서를 우선적으로 참조합니다.
- **전체 개요**: [개요서_업무말투변환기.md](file:///C:/rookies6/vibe_antigravity/biztalk_antigravity/개요서_업무말투변환기.md)
- **요구사항 및 기술 사양**: [PRD_업무말투변환기.md](file:///C:/rookies6/vibe_antigravity/biztalk_antigravity/PRD_업무말투변환기.md)
  - *디렉토리 구조, 기술 스택, API 명세, 프롬프트 전략 등은 PRD 문서에 정의된 스펙을 엄격히 따릅니다.*

---

## 2. Antigravity의 개발 행동 규칙

본 프로젝트를 개발할 때 Antigravity는 아래 규칙을 반드시 준수해야 합니다.

### 📌 1. uv 가상환경 및 의존성 격리 준수
- 백엔드 의존성 패키지는 반드시 `backend/requirements.txt`에 명시된 버전을 기준으로 **`uv` 패키지 관리자**를 사용하여 가상환경(`.venv`) 내에 격리하여 설치해야 합니다. (`uv venv` 및 `uv pip install -r backend/requirements.txt` 사용)
- 시스템 전역 파이썬 환경에 패키지를 임의로 설치하지 마십시오.

### 📌 2. 보안 및 환경 변수 노출 방지
- `.env` 파일은 절대로 무단 수정하지 않으며, API 키(예: `UPSTAGE_API_KEY`) 등의 민감한 자격 증명 정보가 콘솔에 출력되거나 Git 커밋에 포함되지 않도록 철저히 관리합니다.
- `.gitignore` 파일에 `.env` 및 가상환경 관련 디렉토리 등이 정상적으로 등록되어 있는지 항상 검증하십시오.

### 📌 3. 파괴적인 작업의 자동 실행 절대 금지
- `git push --force`, `git reset --hard` 등 원격 커밋 히스토리를 덮어쓰거나 추적되지 않는 파일들을 지우는 비가역적 파괴 명령은 사용자의 명시적 동의 없이는 절대로 자동으로 실행해서는 안 됩니다.

### 📌 4. 단계적 구현 및 로컬 테스트 검증
- 한 번에 다량의 소스코드를 대규모로 수정하지 않고, 하나의 기능 단위로 코드를 작성한 뒤 반드시 로컬에서 서버(`uvicorn main:app --reload`) 및 프론트엔드가 올바르게 연동되어 동작하는지 직접 검증하십시오.
- 에러 발생 시, 원인을 사용자에게 먼저 투명하게 분석하여 보고한 후 수정 단계를 밟아야 합니다.